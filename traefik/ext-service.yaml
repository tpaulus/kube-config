apiVersion: v1
kind: Service
metadata:
  name: traefik-ext
  namespace: traefik
spec:
  ports:
    - name: traefik-ext
      port: 9200
      portocol: TCP
      targetPort: web-ext
  selector:
    app.kubernetes.io/instance: traefik-traefik
    app.kubernetes.io/name: traefik
  type: ClusterIP