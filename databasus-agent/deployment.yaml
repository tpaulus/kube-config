---
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: databasus-agent
  namespace: databasus
spec:
  itemPath: vaults/K3S/items/Databasus-Agent
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: databasus-agent
  namespace: databasus
  labels:
    app: databasus-agent
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: databasus-agent
  template:
    metadata:
      labels:
        app: databasus-agent
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      volumes:
        - name: docker-socket
          emptyDir: {}
        - name: docker-data
          emptyDir: {}
        - name: agent-data
          emptyDir: {}
      containers:
        - name: docker
          image: docker:29.7.2-dind@sha256:12e683a161823b2a839aeea999b9d960e6e1f9a97b1679ad6b441982e2d9cf07
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
          volumeMounts:
            - name: docker-socket
              mountPath: /var/run
            - name: docker-data
              mountPath: /var/lib/docker
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1
              memory: 1Gi
        - name: agent
          image: ghcr.io/databasus/databasus:v3.55.1
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -c
          args:
            - |
              cat > databasus-verification.json <<EOF
              {
                "databasusHost": "https://databasus.ing.k3s.brickyard.whitestar.systems",
                "agentId": "${AGENT_ID}",
                "token": "${AGENT_TOKEN}",
                "maxCpu": 2,
                "maxRamMb": 4096,
                "maxDiskGb": 50,
                "maxConcurrentJobs": 1
              }
              EOF
              until [ -S /var/run/docker.sock ]; do sleep 1; done
              exec /app/agent-binaries/databasus-verification-agent-linux-amd64 run
          workingDir: /var/lib/databasus-agent
          env:
            - name: AGENT_ID
              valueFrom:
                secretKeyRef:
                  name: databasus-agent
                  key: username
            - name: AGENT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: databasus-agent
                  key: password
            - name: DOCKER_HOST
              value: unix:///var/run/docker.sock
          securityContext:
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: docker-socket
              mountPath: /var/run
            - name: agent-data
              mountPath: /var/lib/databasus-agent
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: 2
              memory: 4Gi
